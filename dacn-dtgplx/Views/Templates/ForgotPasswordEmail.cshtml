@model dynamic
@{
    Layout = null;
    string otp = Model.OTP;
    string username = Model.Username ?? "bạn";
}

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8" />
    <title>Mã OTP xác thực</title>
    <style>
        body {
            font-family: 'Segoe UI', Arial, sans-serif;
            background: #f4f6f8;
            margin: 0;
            padding: 0;
        }

        .email-container {
            max-width: 540px;
            margin: 30px auto;
            background: #ffffff;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 6px 22px rgba(0,0,0,0.12);
        }

        .header {
            background: linear-gradient(45deg, #007bff, #0056d6);
            padding: 18px;
            text-align: center;
            color: #fff;
            font-size: 22px;
            font-weight: bold;
        }

        .content {
            padding: 25px 30px;
            color: #333;
        }

        .otp-box {
            background: #f3f8ff;
            padding: 15px;
            margin: 20px 0;
            text-align: center;
            border-left: 5px solid #007bff;
            border-radius: 8px;
            font-size: 32px;
            letter-spacing: 8px;
            font-weight: bold;
            font-family: 'Courier New', monospace;
            color: #0050c8;
        }

        .footer {
            padding: 15px;
            background: #f2f2f2;
            text-align: center;
            color: #666;
            font-size: 13px;
        }

        .highlight {
            color: #007bff;
            font-weight: 600;
        }
    </style>
</head>

<body>
    <div class="email-container">

        <div class="header">
            Xác thực đặt lại mật khẩu
        </div>

        <div class="content">
            <p>Xin chào <strong>@username</strong>,</p>

            <p>Bạn vừa yêu cầu đặt lại mật khẩu cho tài khoản <strong>@Model.Email</strong>.</p>

            <p>Dưới đây là mã OTP của bạn, mã có hiệu lực trong <span class="highlight">60 giây</span>:</p>

            <div class="otp-box">@otp</div>

            <p>Nếu bạn không yêu cầu hành động này, hãy bỏ qua email.</p>

            <p>
                Trân trọng,<br />
                <strong>DT GPLX Support</strong>
            </p>
        </div>

        <div class="footer">
            © @DateTime.Now.Year DT GPLX — Email được gửi tự động, vui lòng không phản hồi.
        </div>

    </div>
</body>
</html>
